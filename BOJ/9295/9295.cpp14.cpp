#include "bits/stdc++.h"
using namespace std;
int main() {
	int Case;
	cin >> Case;
	for (int tc = 1; tc <= Case; tc++) {
		int a, b;
		cin >> a >> b;
		printf("Case %d: %d\n",tc, a + b);
	}
	return 0;
}